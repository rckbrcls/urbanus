# Schemas

Language-neutral contracts (JSON Schema / OpenAPI) will live here when needed.

Currently, the Python and TypeScript codebases share type contracts by convention
(matching field names and structures) rather than code generation.
