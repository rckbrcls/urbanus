/**
 * Context do módulo Map
 *
 * MapContext (Leaflet-based) was removed in the MapLibre migration.
 * State management now uses Zustand stores:
 *   - stores/areaSelectionStore.ts (home page bbox workflow)
 *   - stores/graphStore.ts (project editor graph state)
 *   - stores/commandManager.ts (undo/redo)
 */
