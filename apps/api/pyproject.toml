[project]
name = "urbanus-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "motor",
  "pydantic>=2",
  "rasterio",
  "numpy",
  "httpx",
  "urbanus-geo",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
